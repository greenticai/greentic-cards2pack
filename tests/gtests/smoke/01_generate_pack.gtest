#SET CARDS_DIR=${WORK_DIR}/cards
#SET PACK_DIR=${WORK_DIR}/pack
#MKDIR ${CARDS_DIR}
#WRITE ${CARDS_DIR}/card.json <<<EOF
{
  "type": "AdaptiveCard",
  "actions": [
    {
      "type": "Action.Submit",
      "title": "Next",
      "data": {
        "flow": "demo",
        "step": "step-1"
      }
    }
  ]
}
EOF
#RUN cargo run --quiet -- generate --cards ${CARDS_DIR} --out ${PACK_DIR} --name demo
#EXPECT_EXIT 0
#SAVE_ARTIFACT ${PACK_DIR}/.cards2pack/manifest.json
#SAVE_ARTIFACT ${PACK_DIR}/flows/main.ygtc
#SAVE_ARTIFACT ${PACK_DIR}/dist/demo.gtpack
